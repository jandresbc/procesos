{% extends 'base.html.twig' %}

{% block title %}Edit Procesos{% endblock %}

{% block body %}
    <h1>Editar Proceso</h1>

    {{ include('procesos/_form.html.twig', {'button_label': 'Actualizar'}) }}

    <div class="text-left">
        <a class="btn btn-sm btn-info" href="{{ path('procesos_index') }}">Volver</a>
    </div>

    {# include('procesos/_delete_form.html.twig') #}
{% endblock %}
